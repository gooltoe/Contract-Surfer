{
  "name": "client",
  "version": "1.0.0",
  "description": "the discord bot that takes information from the server and sends it to guilds that are subscribed to the event",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gooltoe",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.7.0",
    "socket.io-client": "^4.5.4"
  }
}
